{
  "name": "task-scheduler",
  "version": "1.0.0",
  "description": "An app that helps users to schedule tasks as a group",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "hotStart": "node --env-file=.env --watch src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
